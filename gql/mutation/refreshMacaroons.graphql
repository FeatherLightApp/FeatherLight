mutation refreshMacaroons {
    refreshMacaroons {
        __typename
        ... on AuthPayload {
            refresh
            access
        }
        ... on Error {
            message
            errorType
        }
    }
}
